[project]
name = "daily-digest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "google-auth>=2.29.0",
    "jinja2>=3.1.0",
]

[tool.vercel]
entrypoint = "api.index:handler"
